spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/finance_tracker?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:Pruthvi@2324}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${LOG_SQL:false}


spring.jpa.properties.hibernate.format_sql=${LOG_SQL:false}

spring.main.web-application-type=servlet
spring.jpa.open-in-view=true

app.jwt.secret=${JWT_SECRET:change_this_in_production_to_a_long_random_string}
app.jwt.expiration-ms=${JWT_EXPIRATION:86400000}

server.port=${SERVER_PORT:8081}

spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# Gemini AI Configuration
gemini.api.key=${AI_GEMINI_API_KEY:AIzaSyC857KW9MjET3GUVWWLCdCK1Id7ganeN6E}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent
gemini.timeout.connect=10
gemini.timeout.read=30

# Modern Java Optimization: Virtual Threads (Java 21+)
spring.threads.virtual.enabled=true

# SpringDoc OpenAPI Documentation
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=alpha
springdoc.swagger-ui.tags-sorter=alpha

# Mail Configuration (Resolves JavaMailSender missing bean error)
spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USER:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
